/**
 * GraphQL query constants to avoid magic numbers
 */
export const GRAPHQL_DEFAULT_LIMIT = 100;
export const GRAPHQL_MAX_LIMIT = 250;

/**
 * Default pagination size for list queries
 */
export const DEFAULT_PAGE_SIZE = GRAPHQL_DEFAULT_LIMIT;

/**
 * Maximum allowed items in a single query
 */
export const MAX_PAGE_SIZE = GRAPHQL_MAX_LIMIT;
